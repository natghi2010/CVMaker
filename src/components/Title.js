function Title({title, subtitle}) {
  return (
    <div>
      <h2 className="stylishTitle">{title}</h2>
      <h5 className="minorTitle">{subtitle}</h5>
    </div>
  );
}

export default Title;


// import React from "react";
